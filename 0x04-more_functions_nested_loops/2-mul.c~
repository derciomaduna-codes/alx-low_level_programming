#include "main.h"
/**
 * mul-return the answer when you multiplty a and b.
 * @a:first number to multiply.
 * @b:second number to multiply.
 * Return: 0 (Success)
 */

int mul(int a, int b)
{
return (a*b);
}
