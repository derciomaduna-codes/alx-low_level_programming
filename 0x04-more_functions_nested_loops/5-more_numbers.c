#include "main.h"
/**
 * more_numbers-prints the numbers, from 0 to 9, followed by a newline.
 * Return: 0 (Success)
 */

void more_numbers(void)
{
int k, i;
for (k = 0; k < 10; k++)
{
for (i = 0 ; i <= 14; i++)
{
printf("%d", i);
}
printf("\n");
}
}
